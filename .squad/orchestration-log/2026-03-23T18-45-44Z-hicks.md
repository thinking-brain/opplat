# Orchestration Log: Hicks (Runtime)

**Session:** PostgreSQL Migration — Runtime Provider & Seam Changes  
**Date:** 2026-03-23T18:45:44Z  
**Status:** COMPLETE  
**Outcome:** All runtime hooks converted to PostgreSQL; thin-host seams established

---

## Summary

Successfully implemented thin-host PostgreSQL runtime seam for MainApp, Sales API, and Inventory API. Runtime configuration, design-time factories, tenant provisioning, and admin tenant queries now use Npgsql exclusively. AppHost and Docker defaults point to PostgreSQL.

---

## Implemented Changes (Phase 2–3)

### 1. DbContext Configuration
**Files Updated:**
- `src/Opplat.MainApp/Program.cs` → `UseSqlServer()` → `UseNpgsql()`
- `src/Opplat.Microservices.Shared/Extensions/ServiceCollectionExtensions.cs` → `UseSqlServer()` → `UseNpgsql()`
- `src/Opplat.MainApp/Data/DesignTimeDbContextFactory.cs` → Updated design-time context to PostgreSQL format

### 2. Tenant Provisioning & Multi-tenancy
**Files Updated:**
- `src/Opplat.MainApp/Services/TenantProvisioningService.cs` → Now uses PostgreSQL for tenant database creation
- `src/Opplat.MainApp/Features/Admin/Queries/GetAdminUsersQuery.cs` → Admin queries use PostgreSQL

**Design Decision:** Tenant database access resolved at request time through Npgsql, allowing tenants to be configured with either:
- Full `ConnectionString` (legacy support during transition)
- Catalog metadata: `DatabaseName` + optional `DatabaseSchema`

### 3. AppHost & Local Development Defaults
- AppHost now provisions PostgreSQL databases for main + tenant slices
- Passes tenant metadata alongside connection strings
- All environment variables updated to PostgreSQL connection format

### 4. Admin Catalog Compatibility
- Catalog resolver can derive database names from both PostgreSQL and legacy SQL Server connection strings
- Allows smooth transition during wider architecture migration

---

## Migration Management

**Legacy SQL Server Migrations Removed:**
- Deleted `20221121050808_Initial.cs` and `.Designer.cs`
- Deleted `20221123213125_RemoveDuende.cs` and `.Designer.cs`
- Deleted `20221123231550_AddCostTabs.cs` and `.Designer.cs`
- Deleted `OpplatDbContextModelSnapshot.cs`

**Rationale:** SQL Server migrations contained TSQL-specific syntax. EF Core will generate appropriate PostgreSQL migrations on first run.

---

## Testing & Validation

- Multi-tenant context resolution verified
- Tenant provisioning works with PostgreSQL
- Admin API functions with PostgreSQL
- Sales and Inventory APIs create schema on startup

---

## Deferred Decisions

- Per-tenant PostgreSQL schemas (currently carrying `DatabaseSchema` metadata only)
- Production secret rotation and connection-string rewrites
- Migration regeneration and provider-specific DDL cleanup timing

---

## Design Rationale

This implementation allows backend runtime to execute on PostgreSQL immediately without baking provider logic into hosts, while preserving catalog-driven multitenancy compatibility during wider architecture migration.
