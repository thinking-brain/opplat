# Bishop Orchestration Log — 2026-03-21 13:19:52

## Scope
Validate scope regression and test seams

## Outcome
✅ Strengthened regression checks and surfaced the contract mismatch needing adjudication.

## Key Findings

1. **Frontend/Keycloak Contract Drift:** Identified that the current repository state shows clear divergence:
   - Both SPA `runtimeConfig.ts` files default to `openid` only
   - Both SPA `auth/oidc.ts` files force only `openid` as required scope
   - README documentation shows `openid` as the default
   - **But** the actual intended contract (per realm and Docker Compose) is `openid profile email offline_access`

2. **Test Harness Created:** Added two new test classes:
   - `FrontendAuthContractTests.cs` — Guards the intended SPA-requested scope contract and verifies both `runtimeConfig.ts` and `auth/oidc.ts` stay aligned
   - `KeycloakRealmContractTests.cs` — Guards that Opplat does not redefine Keycloak built-in OIDC scopes as custom realm scopes

3. **Validation Results:**
   - ✅ `KeycloakRealmContractTests` passes (realm is correct)
   - ❌ `FrontendAuthContractTests` fails (confirms frontend/docs drift)

4. **Contract Adjudication Needed:** Frontend and documentation must be brought to:
   - Request scopes: `openid profile email offline_access`
   - Keep `roles` on realm client default scopes, not in SPA-requested scope string

## Output
Decision file created: `.squad/decisions/inbox/bishop-scope-validation.md`
