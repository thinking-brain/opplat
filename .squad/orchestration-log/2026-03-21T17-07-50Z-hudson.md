# Orchestration Log: Hudson

**Timestamp:** 2026-03-21T17:07-50Z  
**Session:** Live Admin OIDC Storage Cleanup  
**Role:** Infrastructure / Keycloak Diagnostics

## Activities

1. **Live Keycloak Realm State Validation**
   - Queried admin API for `opplat-admin` client configuration
   - Verified `redirectUris` includes `http://localhost:3201` ✅
   - Verified `webOrigins` includes `http://localhost:3201` ✅
   - Confirmed client-scope assignments match frontend scope request (`openid profile email offline_access`) ✅

2. **Direct Token-Endpoint CORS Probe**
   - Sent token request with explicit `Origin: http://localhost:3201` header and `client_id=opplat-admin`
   - Result: ✅ `Access-Control-Allow-Origin: http://localhost:3201` returned
   - Sent identical probe with `client_id=opplat-client` (wrong client)
   - Result: ❌ No ACAO header returned

3. **Decision & Diagnosis**
   - **Finding:** Realm config is NOT the blocker; the live CORS error shape matches stale frontend/browser state (wrong client ID in storage)
   - **Consequence:** Before code changes, operators must reset container state and clear browser cache per runbook

## Learnings Appended to Vasquez History

- The admin SPA's Docker-compose injection of `VITE_AUTH_CLIENT_ID=opplat-admin` is correct; live Keycloak realm already allows the origin
- Browser-side storage pruning on startup is hygiene work to keep future auth debugging cleaner
