# Hicks Orchestration — 2026-03-21T13-48-01Z

## Task
Verify/align admin role and claim contract

## Outcome
✅ Confirmed SuperAdmin policy alignment and broadened claim normalization for Keycloak role payload shapes across backend/shared auth handling.

## Key Changes
- Updated `src/Opplat.MainApp/Auth/AuthClaimTypes.cs` to handle both nested and flat dotted claim shapes
- Updated `src/Opplat.MainApp/Auth/OidcClaimsTransformation.cs` for role normalization
- Updated `src/opplat-admin/src/auth/claims.ts` for consistent claim reading
- Updated `src/opplat-react/src/auth/claims.ts` for consistent claim reading
- Verified `SuperAdmin` role contract is consistent across all auth layers

## Root Cause Identified
Keycloak role claims can arrive in different materialized shapes:
- Nested: `realm_access.roles`, `resource_access.{client}.roles`
- Flat dotted: `realm_access.roles`, `resource_access.{client}.roles`

Backend and frontend claim readers needed to accept both shapes to prevent misalignment during OIDC token processing.

## Validation
- Backend role validation tests passing
- Claims normalization covers both payload shapes
