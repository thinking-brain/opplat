# Orchestration: Vasquez — Admin Auth Runtime Brittleness Fix

**Date:** 2026-03-22T11:46:18Z  
**Agent:** Vasquez (Frontend Runtime)  
**Session:** Admin auth simplification validation  

## Mission
Fix frontend runtime brittleness in admin `AuthContext` bootstrap by deduplicating in-flight session restore, tolerating transient CSRF bootstrap failure, and preventing React dev double-mount crashes.

## Deliverables

✅ **Session Restore Deduplication:**
- Deduplicate `restoreSession()` with shared in-flight promise (`sessionRestorePromise` ref)
- Prevents React 18 StrictMode double-mount from amplifying transient backend failures
- Reduces spurious session restore retries during dev

✅ **CSRF Bootstrap Failure Tolerance:**
- Separate CSRF token fetch from session restoration critical path
- Session restore succeeds even if `/admin/session/csrf` temporarily unavailable
- `buildCsrfHeaders()` re-fetches CSRF token lazily on first mutating request if bootstrap fails
- Matches backend design: CSRF tokens are already re-acquired per-request anyway

✅ **Validation:**
- `npm run lint` — passed
- `npm run build` — passed
- No regressions in auth context behavior

## Outcome

Admin auth bootstrap is now resilient to transient backend failures and React dev-mode double-mounts. Session restore deduplication eliminates bootstrap noise during development. CSRF bootstrap failure is tolerated gracefully with lazy reacquisition. Coordinator validation: all tests green (`AuthEndpointAuthorizationIntegrationTests` 17/17), both SPAs lint/build pass.
