# Hicks — 2026-03-22T11:41:13Z

## Task: Admin API Compose Startup Failure Fix

### Status: ✅ COMPLETE

### Work Summary
Reproduced and fixed the admin API Docker Compose startup failure by removing the duplicate minimal API `/health` mapping that created an `AmbiguousMatchException`. The `/health` endpoint is now served exclusively by `HealthController.Get()`.

### Decisions Made
- **Decision:** Treat the dedicated admin API compose startup failure as a backend routing defect, not an auth/compose wiring issue. Keep `/health` served only by `HealthController` and remove the duplicate minimal API `/health` mapping.
- **Rationale:** Docker Compose was reporting service failure because health check hit `/health`, and ASP.NET Core matched both `HealthController.Get` and `app.MapGet("/health", ...)`, producing `AmbiguousMatchException`.

### Changes
- File: `src/Services/Admin/Opplat.Services.Admin.Api/Program.cs`
  - Removed: Duplicate minimal API `/health` mapping
  - Preserved: Full admin BFF/session contract (`/admin/session/*`, `/auth/bff/admin/*`)

### Validation
- ✅ `dotnet build ./src/Services/Admin/Opplat.Services.Admin.Api/Opplat.Services.Admin.Api.csproj`
- ✅ `docker compose up -d --build admin-api` → service reaches healthy state
- ✅ `GET http://localhost:8084/health` returns HTTP 200 Healthy|admin

### Coordination
- **Hudson:** Verified compose/container startup behavior and confirmed health endpoint should be explicit and anonymous
- **Bishop:** Added focused startup regression coverage and validated the fix
