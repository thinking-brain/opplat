# Bishop Orchestration — 2026-03-21T13-48-01Z

## Task
Validate post-login regression and add coverage

## Outcome
✅ Pinned issue to callback handoff/loading gate, added regression coverage, and validated tests/builds successfully.

## Key Changes
- Updated `test/Opplat.MainApp.Test/Auth/FrontendAuthContractTests.cs` with post-login callback coverage
- Verified callback completion uses hard navigation (`window.location.replace`) instead of soft replaceState
- Confirmed loading gate does not block when OIDC user is already restored
- Added test cases covering both callback paths (with and without navigator bookkeeping)

## Root Cause Identified
The post-login callback completion seam had two issues:
1. `activeNavigator` state lingered briefly even after `oidc.user` was available, blocking the loading gate
2. Soft URL rewrite (`replaceState`) didn't notify `BrowserRouter` of route change

## Validation
- All backend auth tests passing: `dotnet test --filter Auth`
- Callback contract tests added to prevent regression
- Builds validated: `dotnet build` and both frontend `npm run build` commands
