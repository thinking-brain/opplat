# Ripley Orchestration Log — 2026-03-21 13:19:52

## Scope
Adjudicate conflicting scope contract conclusions from Hicks, Vasquez, and Bishop

## Outcome
✅ Finalized the end-to-end contract as `openid profile email offline_access` with roles injected via claims, and applied minimal repo corrections.

## Key Findings

1. **Multi-Layer Analysis Completed:**
   - **Keycloak Realm Layer:** Correctly configured with `profile`, `email`, `roles`, `opplat-tenancy`, `opplat-api-audience` as defaults; `offline_access` as optional
   - **Docker Compose Layer:** Already had correct `VITE_AUTH_SCOPE=openid profile email offline_access`
   - **Frontend Code Layer:** Defaulted to `openid` only (drift from Compose contract)
   - **Documentation Layer:** Showed `openid` only (drift from Compose contract)

2. **Root Cause Determination:** The `roles` scope error originates from stale configuration (old `.env.local` or browser state), not from current code. The intended contract has always been `openid profile email offline_access` in Docker Compose, but frontend sources drifted.

3. **Authoritative Contract Decision:**
   ```
   openid profile email offline_access
   ```
   - `openid` — mandatory OIDC
   - `profile` — name/nickname claims (Keycloak default, harmless to request)
   - `email` — email claims (Keycloak default, harmless to request)
   - `offline_access` — refresh tokens (MUST be requested; optional in Keycloak)
   - **NO `roles`** — Keycloak injects via defaultClientScopes automatically

4. **Corrections Applied:**
   - `src/opplat-react/src/runtimeConfig.ts` — Updated fallback default to correct scope
   - `src/opplat-admin/src/runtimeConfig.ts` — Updated fallback default to correct scope
   - `src/opplat-react/.env.example` — Updated documentation
   - `src/opplat-admin/.env.example` — Updated documentation
   - `README.md` — Updated local dev examples and env reference table

5. **Validation Completed:**
   - TypeScript compilation: ✅ Both SPAs pass `tsc --noEmit`
   - Docker Compose files: Already correct, no changes needed

## Output
Decision file created: `.squad/decisions/inbox/ripley-scope-adjudication.md`
